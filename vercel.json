{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/notion-proxy/:path*", "destination": "https://api.notion.com/:path*" }
  ]
}
